#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

<<<<<<< HEAD
echo "Running post-merge hooks"
if git diff --name-only | grep --quiet "yarn.lock"
then
  echo "Yarn.lock has changed, updating local dependencies"
  yarn install
  exit 0
fi

if git diff --name-only | grep --quiet "Gemfile.lock"
then
  echo "Gemfile.json has changed, updating local dependencies"
  bundle install
  exit 0
fi

if git diff --name-only | grep --quiet "schema.rb"
then
  echo "schema.rb has changed, running migrations"
  rails db:migrate
  exit 0
fi
echo "Finish running post-merge hooks"
=======
bundle install
yarn install
rails db:migrate
>>>>>>> d83d370 (Config: Add post merge hook)
